{
  "name": "r2-signer",
  "version": "1.0.0",
  "description": "Cloudflare Worker for generating presigned Cloudflare R2 URLs.",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}